[/Script/EngineSettings.GeneralProjectSettings]
ProjectID=SDT2026
ProjectName=Swarm Defense Trainer
CompanyName=TLS Intern Team
ProjectVersion=1.0.0.0

; =============================================================================
; Hardware / Input Configuration
; =============================================================================
; InputMode controls whether the game uses mouse+keyboard or physical hardware.
;   MockInput     = Mouse to aim, Left-click to fire (DEFAULT — no hardware needed)
;   HardwareInput = VN-100 IMU to aim, Arduino trigger to fire
;
; For development, keep this set to MockInput. Switch to HardwareInput only
; when testing with physical hardware connected.
; =============================================================================

[/Script/SwarmDefenseStarter.SDTStarterSettings]
InputMode=MockInput
OrientationSerialPort=COM3
TriggerSerialPort=COM4
BaudRate=115200
YawSensitivity=1.0
PitchSensitivity=1.0
SmoothingAlpha=0.3
DeadZoneDegrees=1.0
FireCooldown=0.15
